import 'dart:io';

import 'package:neu_app/network/api_service.dart';

class YouthNeuRepository {
  // Stream getUsers() {
  //   return ApiService.getInstance().get(url: "users").map((responce) =>
  //          (responce as List).map((item) => User.fromJson(item)).toList()
  //       );
  // }
}
